# Enable legacy peer dependency resolution
# Required for react-animated-slider@2.0.0 which expects react@^16.4.0
# Project uses react@18.2.0, causing peer dep conflict
# This flag allows npm to proceed despite the conflict
legacy-peer-deps=true
